defaults:
  - dataset: pusht
  - model: baseline
  - training: default
  - evaluation: default
  - _self_

seed: 42
output_dir: ./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

hydra:
  run:
    dir: ${output_dir}
  job:
    chdir: false
