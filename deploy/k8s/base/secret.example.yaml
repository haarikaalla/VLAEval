# Placeholder Secret manifest. DO NOT commit real secret values.
# In production, generate this via a secrets manager (e.g. External Secrets
# Operator, Sealed Secrets, or `kubectl create secret` in a secure pipeline)
# rather than checking plaintext values into version control.
apiVersion: v1
kind: Secret
metadata:
  name: vla-eval-secrets
  namespace: vla-eval
type: Opaque
stringData:
  APP_SECRET_KEY: "REPLACE_ME_WITH_SECURE_RANDOM_VALUE"
  API_DEFAULT_API_KEY: "REPLACE_ME_WITH_SECURE_RANDOM_VALUE"
  DATABASE_URL: "postgresql+psycopg2://vla_eval:REPLACE_ME@vla-eval-postgres:5432/vla_eval"
  HF_TOKEN: ""
