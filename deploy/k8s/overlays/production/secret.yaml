# DO NOT commit real secrets. Populate via `kubectl create secret` in CI/CD
# using values from your secrets manager, or use Sealed Secrets/External Secrets.
apiVersion: v1
kind: Secret
metadata:
  name: vla-eval-secrets
  namespace: vla-eval
type: Opaque
stringData:
  APP_SECRET_KEY: "REPLACE_ME"
  API_DEFAULT_API_KEY: "REPLACE_ME"
  DATABASE_URL: "postgresql+psycopg2://vla_eval:REPLACE_ME@vla-eval-postgres:5432/vla_eval"
  MLFLOW_BACKEND_STORE_URI: "postgresql+psycopg2://vla_eval:REPLACE_ME@vla-eval-postgres:5432/mlflow"
  POSTGRES_PASSWORD: "REPLACE_ME"
  HF_TOKEN: ""
